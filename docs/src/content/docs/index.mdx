---
title: use-legend
description: Observable-native React utility hooks built on Legend-State. Inspired by VueUse and react-use.
template: splash
hero:
  tagline: Inspired by VueUse and react-use — reimagined for observable-first reactivity with Legend-State. Every hook returns observables, enabling fine-grained updates without re-rendering the component tree.
  actions:
    - text: Get Started
      link: /use-legend/guides/example/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/TigerWest/use-legend
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Why use-legend?

[VueUse](https://vueuse.org/) and [react-use](https://github.com/streamich/react-use) proved that a composable utility collection can dramatically improve developer experience. `use-legend` brings that same philosophy to the observable world — every hook is designed from the ground up to work with [Legend-State](https://legendapp.com/open-source/state/) observables, delivering fine-grained reactivity instead of whole-component re-renders.

## Packages

<CardGrid>
  <Card title="@usels/core" icon="puzzle">
    Observable-native hooks for DOM and browser interaction — element refs, resize, intersection, scroll, mouse, media queries, and more. Every hook accepts `Ref$` for reactive element targeting.
  </Card>
  <Card title="@usels/integrations" icon="rocket">
    third-party integration built for Legend-State
  </Card>
  <Card title="@usels/vite-plugin-legend-memo" icon="setting">
    Vite plugin that automatically wraps `obs$.get()` calls in JSX with `<Memo>` at build time — no manual wrapping needed. Place before `@vitejs/plugin-react` in your config.
  </Card>
  <Card title="@usels/babel-plugin-legend-memo" icon="seti:config">
    Babel plugin that transforms `obs$.get()` into fine-grained `<Memo>` boundaries. Use directly with babel or via the Vite plugin wrapper.
  </Card>
</CardGrid>
